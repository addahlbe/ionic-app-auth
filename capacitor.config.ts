import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.appauth',
  appName: 'ionic-appauth',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
